# Core & LangGraph
langgraph
langchain-openai
openai
tiktoken

# Data Processing & ML
numpy
pandas
scikit-learn>=1.3.0
hdbscan
kiwipiepy
lightgbm
torch>=2.0.0
# mlflow(실험 추적)/optuna(하이퍼파라미터 탐색)는 main_lgbm.py/tune_hyperparams.py에서
# 선택적으로만 import되는 optional 의존성이라 여기 기본 목록에는 올리지 않는다
# (recommend_engine/pyproject.toml의 tracking/tuning extra 참고, pip install -e ".[tracking,tuning]")

# Database
psycopg2-binary>=2.9.11
python-dotenv>=1.2.1
sqlalchemy>=2.0.46
pgvector>=0.4.2

# Crawler & Utils
feedparser
requests
python-dateutil
trafilatura
tqdm>=4.65.0
pyyaml
lxml_html_clean

# Embedding Model
FlagEmbedding==1.2.5
transformers==4.38.2
sentence-transformers==2.2.2

# Airflow는 뺐다(스케줄러는 supercronic + `python -m jobs.run`, docs/adr/0006).
# 보관된 DAG(backend/airflow/dags)을 직접 띄워 보려면: pip install -r backend/requirements-airflow.txt
