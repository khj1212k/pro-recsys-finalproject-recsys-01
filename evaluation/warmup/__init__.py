"""2026-09-26 첫 E2E 워밍업 실행 도구 (임베딩 → 클러스터링 → LangGraph 생성).

실행 스크립트는 로컬 compose DB(DB_* 환경변수)와 실제 모델/LLM을 쓰지만, 수치 계산·예산
판단·클러스터 선택 같은 로직은 순수 함수로 두고 tests/evaluation/test_warmup_*.py에서 검증한다.
리포트에는 기사 본문을 남기지 않는다(id·sha256·개수만).
"""
