[pytest]
testpaths = tests
# 저장소 루트의 jobs/ 패키지를 `python -m pytest`가 아닌 `pytest`로 실행해도 임포트할 수 있게 한다.
pythonpath = .
markers =
    integration: 실제 DB/네트워크/외부 서비스가 필요한 테스트 (기본 CI 실행에서는 제외)
    benchmark: 실행 시간 비교 벤치마크 (느리고 하드웨어 의존적이라 CI에서는 제외)
